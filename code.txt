conversion_rates.insert("THB", {
    let mut other_rates = HashMap::new();
    other_rates.insert("GBP", <thb_to_gbp_conversion_rate>);
    other_rates.insert("USD", 0.0);  // Placeholder for future implementation
    other_rates
});

conversion_rates.insert("GBP", {
    let mut other_rates = HashMap::new();
    other_rates.insert("THB", <gbp_to_thb_conversion_rate>);
    other_rates.insert("USD", 0.0);  // Placeholder for future implementation
    other_rates
});