use std::collections::HashMap;

fn main() {
    //  A hashmap with string slices (&str) as keys and another hashmap as values:
    let mut conversion_rates: HashMap<&str, HashMap<&str, f64>> = HashMap::new();

    println!("Hello, World!");
}
